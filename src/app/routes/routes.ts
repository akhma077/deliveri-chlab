interface RoutePaths {
    home: string;
    restaraunt: string;
    basket: string;
}

export const routePaths: RoutePaths = {
    home: "/",
    restaraunt: "restaraunt/:id",
    basket: "basket",
};
